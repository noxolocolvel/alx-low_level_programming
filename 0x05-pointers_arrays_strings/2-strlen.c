#include "main.h"
#include <stdio.h>

/**
 * _strlen - function that the length of a string
 * @s: pointer
 * return: void
*/

int _strlen(char *s)
{
	int = 0;

	while (s[i] != '\0')
	{
		i++;
	}

	return (i);
}
